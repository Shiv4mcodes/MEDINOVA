// src/styles/globalStyles.js
// Export either a string of CSS or an array of Chakra style objects
const someGlobalStyles = `
  @layer base {
    body {
      margin: 0;
      padding: 0;
      font-family: system-ui, sans-serif;
    }
  }
`;

export default someGlobalStyles;
